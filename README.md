

**Install-Package TasqR** Using TasqR for handling CQRS - extension made by us too :)   
**Install-Package Microsoft.EntityFrameworkCore** for ORM (both handles MSSQL and SQLite providers in this test project)


```
--------------> see comments on the right

├───Docs                                                    # where docs, reference, links, etc
└───Src                                                     # all codes related to the project
    ├───Core                                                # core codes (Base Project, Application Layer, Persistence Layer, Infrastructure)
    │   ├───TestProj (ns2.1)                                # this is the base project, you can pass this around to any project types in Presentation Layer (super less dependencies)
    │   │   ├───Commands                                    # CQRS (Commands) - handlers are in Application Layer (TestProj.Application)
    │   │   │   └───EmployeeCmds
    │   │   ├───Constants
    │   │   ├───Domain                                      # most likely contains models, view models if you want it to be cross app available
    │   │   │   └───Entities                                # └─ entities defines the model mapping with your database tables and other objects
    │   │   ├───Enums
    │   │   ├───Exceptions
    │   │   ├───Interfaces
    │   │   └───Queries                                     # CQRS (Queries) - handlers are in Application Layer (TestProj.Application)
    │   │       └───EmployeeQrs
    │   ├───TestProj.Application  (ns2.1)                   # basic logic for the application layer (usually default implementation of how the application behave)
    │   │   ├───Common
    │   │   │   └───Extensions
    │   │   └───Handlers
    │   │       ├───Commands
    │   │       │   └───EmployeeCmds
    │   │       └───Queries
    │   │           └───EmployeeQrs
    │   └───TestProj.Infrastructure  (ns2.1)                # this is where you define the dependencies for external resources shared across application
    │       └───Persistence                                 # this is also the project where you store the definition on how you want your database gets setup
    │           └───Configurations
    │               └───Base
    ├───Migrations                                          # migration projects (generated by ef design via PMC).
    │   ├───TestProj.DbMigration.SQLite  (ns2.1)            # └─ it should be different project per provider because not all providers implement the same architecture
    │   │   └───Migrations
    │   └───TestProj.DbMigration.SqlServer  (ns2.1)
    │       └───Migrations
    ├───Presentation                                        # presentation (web, desktop app, console, mobile app, services)
    │   └───Web
    │       └───TestProj.WebAPI (net5.0)
    │           ├───Controllers                             # you can override your handlers here if you want custom behavior (eg. Accessing cache instead of database) 
    │           ├───Properties                              # └─ the beauty of this architecture is you can just override the default handler and you dont need to repeat the logic
    │           └───ViewModels
    └───Tests                                               # and finally, unittesting
        └───CommandsTests (net5.0)
            └───Common